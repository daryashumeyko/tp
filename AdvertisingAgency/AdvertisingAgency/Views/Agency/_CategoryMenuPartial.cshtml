@model IEnumerable<AdvertisingAgency.Models.ViewModels.Agency.CategoryVM>

<h3>Категории</h3>

@foreach (var item in Model)
{
    <a class="list-group-item list-group-item-action" href="/agency/category/@item.Slug.ToLower()">@item.Name</a>
}
